{
  "name": "reactify-express",
  "version": "1.0.0",
  "main": "../server/index.js",
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A simple way to use a React app as a static file served by Express.js.",
  "scripts": {
    "setup": "cd server && npm install && cd ../app && npm install",
    "build": "cd app && npm run build",
    "start": "npm run build && cd server && node index.js"
  }
}
